
import { FunctionComponent } from 'preact'

export const route_options: RouteOptions = {
  altUrl: '/mock/:param',
  name: 'mock-view',
}

const mockviews_mockparam_index_route: FunctionComponent = props => {
  return (
    <></>
  )
}

export default mockviews_mockparam_index_route