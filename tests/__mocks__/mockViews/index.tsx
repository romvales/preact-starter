
import { FunctionComponent } from 'preact'

export const route_options: RouteOptions = {
  name: 'mock-view',
}

const mock_views_route: FunctionComponent = props => {
  return (
    <></>
  )
}

export default mock_views_route